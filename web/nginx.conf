worker_processes 1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location = / {
            rewrite / /resume;
        }

        location /resume {
            proxy_pass http://resume;
        }
    }
}